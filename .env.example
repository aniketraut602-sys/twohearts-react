# ==============================================
# TwoHearts Application Environment Variables
# ==============================================

# CRITICAL: JWT Secret Key
# This MUST be set to a strong, random string in production
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# ==============================================
# API Server Configuration (Port 3000)
# ==============================================

# Database URL for API server (SQLite)
# Format: sqlite:/path/to/database.db or :memory: for in-memory database
DATABASE_URL=sqlite:./data/twohearts.db

# ==============================================
# Backend WebSocket Server Configuration (Port 3001)
# ==============================================

# Database URL for Backend server (PostgreSQL)
# Format: postgresql://username:password@host:port/database
# Example: postgresql://postgres:password@localhost:5432/twohearts
# Leave empty if using SQLite for both servers
BACKEND_DATABASE_URL=

# Server Port (default: 3001)
PORT=3001

# ==============================================
# Frontend Configuration
# ==============================================

# API URL for WebSocket connections
# In development: http://localhost:3001
# In production: https://your-domain.com
VITE_API_URL=http://localhost:3001

# ==============================================
# Production Settings
# ==============================================

# Node Environment (development, production, test)
NODE_ENV=development

# Enable/Disable logging
LOG_LEVEL=info

# ==============================================
# Security Notes
# ==============================================
# 1. NEVER commit .env files to version control
# 2. Always use strong, unique JWT_SECRET in production
# 3. Use HTTPS in production for all connections
# 4. Regularly rotate JWT_SECRET and invalidate old tokens
# 5. Use environment-specific .env files (.env.production, .env.development)