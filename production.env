# Production Environment Variables
# Rename to .env on the server

# Security
# Generate a new secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_STRING

# Database
DATABASE_URL=sqlite:./data/twohearts.db

# Server
PORT=3000
NODE_ENV=production

# Frontend
# Update this to the actual server IP or domain
VITE_API_URL=http://YOUR_SERVER_IP:3000
